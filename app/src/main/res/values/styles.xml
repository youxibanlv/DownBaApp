<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        &lt;!&ndash; Customize your theme here. &ndash;&gt;
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowNoTitle">true</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>-->
    <style name="TabView">
        <item name="android:gravity">bottom|center_horizontal</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:background">@mipmap/tab_bg</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/tab_text_selector</item>
        <item name="android:textSize">12sp</item>
        <item name="iconWidth">28dp</item>
        <item name="iconHeight">28dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:drawablePadding">2dp</item>
    </style>
    <!-- 自定义进度条 -->
    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">50dip</item>
        <item name="android:minHeight">8dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/progressbar_mini</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="tabView">@style/TabView</item>
    </style>
    <!-- 选中状态 -->
    <style name="RatingBar.Select" parent="@android:style/Widget.RatingBar">
        <item name="android:maxHeight">@dimen/wdp18</item>
        <item name="android:progressDrawable">@mipmap/ratingbat_select</item>
        <item name="android:minHeight">@dimen/wdp21</item>
    </style>
    <!-- 未选中状态 -->
    <style name="RatingBar.unSelect" parent="@android:style/Widget.RatingBar">
        <item name="android:maxHeight">@dimen/wdp18</item>
        <item name="android:progressDrawable">@mipmap/ratingbar_unselect</item>
        <item name="android:minHeight">@dimen/wdp21</item>
    </style>
    <!-- 选中状态大星星 -->
    <style name="RatingBar.Select.big" parent="@android:style/Widget.RatingBar">
        <item name="android:maxHeight">@dimen/wdp29</item>
        <item name="android:progressDrawable">@mipmap/ratingbat_select_big</item>
        <item name="android:minHeight">@dimen/wdp29</item>
    </style>
    <!-- 未选中状态大星星 -->
    <style name="RatingBar.unSelect.big" parent="@android:style/Widget.RatingBar">
        <item name="android:maxHeight">@dimen/wdp29</item>
        <item name="android:progressDrawable">@mipmap/ratingbar_unselect_big</item>
        <item name="android:minHeight">@dimen/wdp29</item>
    </style>
    <!--popupWindow 动画样式-->
    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popushow_anim</item>
        <item name="android:windowExitAnimation">@anim/popuhidden_anim</item>
    </style>
</resources>
